<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mystepsapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyStepsApp">
        <activity android:name=".EnterLeague"></activity>
        <activity android:name=".DeleteAccount" />
        <activity android:name=".MyProfile" />
        <activity android:name=".MyAchievments" />
        <activity android:name=".LeagueOver" />
        <activity android:name=".DailyStepsTarget" />
        <activity android:name=".FriendStats" />
        <activity android:name=".FriendInbetween" />
        <activity android:name=".FriendList" />
        <activity android:name=".LeagueTable" />
        <activity android:name=".FindUsers" />
        <activity android:name=".Login" />
        <activity android:name=".Register">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>